# Memlog — FindBack UX (ux-FindBack-2026-08-29)

## Source inputs

- `file:_bmad-output/planning-artifacts/briefs/brief-FindBack-2026-08-28/brief.md` (status: final)
- `file:_bmad-output/planning-artifacts/prds/prd-FindBack-2026-08-28/prd.md` (status: final)

## Notes

- The locked Product Brief explicitly defers Brand and visual identity to the UX phase: *"FindBack's brand, logo, color palette, and visual identity are a related concern and will be defined separately as part of the design phase. This brief intentionally stops at the product level."* No baseline-brief tokens exist to preserve — DESIGN.md must author the visual identity from first principles while honoring Simple / Trustworthy / Clear.
- The PRD is finalized (FR-1..FR-39). EXPERIENCE.md must consume, not redefine.
- A1..A6 PRD-author assumptions are kept as-is; UX does not silently resolve architecture-owned open questions (OQ-1..OQ-5) that belong to PM or Architecture.

## Decisions (Discovery)

- **2026-08-29 — Brand feeling.** Warm & trustworthy. Friendly, human, reassuring. Rounded, organic logo. Warm neutrals + single warm accent. Tone: "we help neighbors get their stuff back."
- **2026-08-29 — Type naming.** Use PRD verbatim — "Lost item" / "Found item" on primary surfaces (buttons, filters, badges, detail headers). No second-person shortcuts on these surfaces.
- **2026-08-29 — Dark mode.** First-class peer to light. Initial theme follows `prefers-color-scheme`. Toggle exposed in header / account menu. Choice persisted in `localStorage`. Logo ships in light + dark variants.
- **2026-08-29 — Reviewer Gate.** Rubric walker (Pass 1) + accessibility lens (WCAG 2.1 AA per PRD NFR). Generic / structure / adversarial lenses skipped at Finalize; trace covered inline.
- **2026-08-29 — UI system [ASSUMPTION].** Not specified in PRD; no shadcn / MUI / etc. implied. DESIGN.md provides token semantics; framework choice is an Architecture decision.
- **2026-08-29 — Form-factor [ASSUMPTION].** Web application only (PRD §5 non-goal). Responsive across desktop, tablet, mobile web.

## Finalization

- **2026-08-29 — event: spines finalized.** DESIGN.md and EXPERIENCE.md status set to `final`. Both reviewers' findings resolved (0 critical, 0 high, 0 medium remaining). Validation report at `./validation-report.md`; per-lens reports at `review-rubric.md` and `review-accessibility.md`.
- **2026-08-29 — UX recommendation (OQ-4).** Per-claim thread should close (read-only) at `Returned` as well as Approved / Rejected / Closed. Forwarded to PM via EXPERIENCE.md §13.5.
- **2026-08-29 — UX recommendation (OQ-5).** Auto-rejection email and rejection screen use softer user-facing copy ("Another claim on this item was approved. Contact the administrator if you think this is a mistake."). Audit reason ("another claim was approved for this item") preserved verbatim per PRD FR-17. Documented in EXPERIENCE.md §4.4.

## Update — Mockup phase

- **2026-08-29 — Update intent: render interactive HTML mockups.** Spines remain finalized; the mockup work promotes `.working/` artifacts into `mockups/`. Source of truth remains PRD + DESIGN.md + EXPERIENCE.md; no spine edits during this phase. Per skill workflow: "Mock coverage confirmed" + "Layout extracted, artifacts promoted" path.
- **2026-08-29 — Logo asset policy.** DESIGN.md §Logo defined the visual direction (rounded squircle "FB" monogram + wordmark, terracotta on light / brighter accent on dark) but did not generate SVG files. The mockup phase creates one shared `assets/logo-mark.svg` and `assets/logo-wordmark.svg` (with theme-aware color via `currentColor`), referenced by every mockup. Architecture owns the production asset pipeline.
- **2026-08-29 — Mockup directory structure.** `mockups/{assets,public,member,admin,states,modals}`. `index.html` is the prototype guide / flow map. Mockups reference shared `assets/tokens.css` for theme + tokens + components.
## Update — Final correction & validation pass (2026-09-06)
- (event) Performed final correction pass on the prototype per reviewer request.
  Key changes: (1) Production Account menu now contains only Sign out; Switch
  persona / Reset demo data / Demo credentials moved to a clearly-labeled
  Prototype-only Controls panel mounted via [data-prototype-controls-slot].
  (2) Submit-claim CTA is now restricted to Found items only (PRD FR-15;
  EXPERIENCE.md §87, §679, §700). State-layer submitClaim() refuses Lost,
  self, admin, and ineligible callers. (3) Reporter display name and
  return-info fields are gated by `hasRelationship` (EXPERIENCE.md §11.1).
  (4) Claim thread read-only lifecycle is enforced both in the UI and at
  the state API: writable while Pending or Approved; read-only once the
  claim is Rejected or the item is Returned / Closed (PRD FR-26; OQ-4).
  (5) Registration rejection length source: EXPERIENCE.md §1028 specifies
  ≥ 5 chars; the previously invented 500-char upper bound was removed.
  (6) Pending users for seeded registrations now mirror registerAccount(),
  so admin approve/reject flips a real user record's accountState and the
  login-blocked state surfaces correctly.
- (event) End-to-end journey tests added at ./journey-tests.js. Result:
  12/12 PASS, including UJ-1..UJ-4 with substitute-receiver branch.
- (event) Link audit re-run (./audit-links.js): 0 issues across 42 HTML
  files / 491 internal references. All asset paths return HTTP 200.
- (assumption) Prototype Guide (index.html) is the canonical entry point
  for reviewers; admin/member pages deliberately do not include the
  Prototype-only Controls panel so the production UX is not interrupted.
